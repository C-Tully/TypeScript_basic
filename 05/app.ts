// // The following file is used in tandem
// // with the tsconfig.json file to show
// // how different settings can change the envionrment
// // when it comes to building

// const button = document.querySelector("button")!;

// function clickHandler(message: string) {
//   console.log("clicked:: " + message);
// }

// function add(n1: number, n2: number) {
//   if (n1 + n2 > 0) {
//     return n1 + n2;
//   }
//   //noEmplicitReturns in tsConfig will require a return in ALL cases
//   return;
// }

// if (button) {
//   button.addEventListener(
//     "click",
//     clickHandler.bind(null, "click handler message")
//   );
// }
